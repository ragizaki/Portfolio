import React from 'react';
import { Nav, NavContainer } from './NavbarStyles';

type Props = {};

const Navbar = () => {
    return (
        <Nav>
            <NavContainer>Hello</NavContainer>
        </Nav>
    );
};

export default Navbar;
