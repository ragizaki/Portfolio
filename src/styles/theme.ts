const theme: ThemeInterface = {
    primary: '#1e96fc',
    white: '#fff',
    black: '#000',
    hover: '#3da5d9',
};

export interface ThemeInterface {
    primary: string;
    white: string;
    black: string;
    hover: string;
}

export default theme;
